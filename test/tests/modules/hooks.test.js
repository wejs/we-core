describe('we.hooks', function () {
  it('should add one hook function with we.hooks.on');
  it('should run all registered functions with we.hooks.trigger');
  it('should remove one functions by name with we.hooks.off');
});